<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>View recipe</h2>

<%= form_for @recipe, :url => casein_recipe_path(@recipe) do |f| %>

	<%= render :partial => 'casein/recipes/form', :locals => { :f => f } %>
	
	<p class="submits">
	  	<%= link_to 'Cancel', casein_recipes_path, :class => 'btn btn-mini btn-danger' %>
		<%= render :partial => 'casein/layouts/approval_q_buttons', :locals => { :f => f } %>
	</p>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Back to list".html_safe, casein_recipes_path %></li>
	<%= simple_form_for @recipe, :url => casein_recipe_path(@recipe), html: {:id => 'stateform'}, remote: true do |f| %>
		<%= render partial: 'casein/layouts/status_form', :locals => {:f => f, :record => @recipe } %>
	<% end %>
<% end %>
