<!-- Scaffolding generated by Casein v5.1.1.5 -->

<h2>Offers<%= casein_pagination_details @offers %></h2>
<%= form_tag edit_multiple_casein_offers_path, method: :post do %>
	<%= render :partial => 'casein/offers/table', :locals => { :offers => @offers } %>
	<%= render :partial => 'casein/layouts/multi_btns' %>
<% end %>

<div class="text-center">
	<%= will_paginate @offers %>
</div>

<%= content_for :sidebar do %>

	<li><%= link_to "#{casein_show_icon "plus-sign"}Add a new offer".html_safe, new_casein_offer_path %></li>
	Download Data: <%= link_to 'CSV', casein_offers_path(format: 'csv') %> | 
		<%= link_to 'Excel', casein_offers_path(format: 'xlsx') %>
	<br />
	<br />
	Import Data with CSV file:
	<br />
		<%= form_tag import_casein_offers_path, multipart: true do %>
			<%= file_field_tag :file, accept: 'text/csv' %>
			<%= submit_tag "Import", :class => 'btn btn-xs btn-primary pull-right' %>
		<% end %>


<% end %>
