$('#stateform select').hide();
$('#state-status').show();
$('#icon-span').append("<%= j image_tag state_icon(@partner_asset.workflow_state), :class => 'state-icon' %>");
$('#current-state').html('<%= j @partner_asset.current_state.to_s %>');
var currentState = $('#current-state').text();
$('#update-date').html('<%= j @partner_asset.updated_at.to_s %>');
buttonChange();
